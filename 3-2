#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main(int argc, char const* argv[]) {
    char a, b;

    scanf("%d %d", &a, &b);

    printf("%d\n", a + b);  // 덧셈
    printf("%d\n", a - b);  // 뺄셈
    printf("%d\n", a * b);  // 곱셈
    printf("%d\n", a / b);  // 나눗셈 (몫)
    printf("%d\n", a % b);  // 나눗셈 (나머지)
    return 0;
}
